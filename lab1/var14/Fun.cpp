#include "Fun.h"


double simple(double x){
	/*int check = 0;
	double res = x;
	if (x == 0)
		return 0;
	else if (x > 0)
		for (int i = x; i >= PI; i -= PI) {
			res -= PI;
			check++;
		}
	else
		for (int i = x; i <= -PI; i += PI) {
			res += PI;
			check++;
		}*/
	return PI;
}
